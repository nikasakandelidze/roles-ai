---
# db-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: roles-ai-db-service
spec:
  type: NodePort
  selector:
    app: roles-ai-db
  ports:
    - name: "postgres"
      protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30432
